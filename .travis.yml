language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t chrisdberry/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true chrisdberry/docker-react npm run test
